# dependencies / build output
node_modules/
dist/
.vite/
RCSprint-web.zip
# electron-builder output (the .exe ships as a GitHub Release asset, never committed)
release/

# local env / OS
*.local
.env
.DS_Store
Thumbs.db

# tooling
.playwright-mcp/
.claude/*.lock
.claude/scheduled_tasks.lock

# verification screenshots & reference images (root only — keeps public/**/*.jpg)
/*.png
/*.jpeg
/*.jpg
/ref-*.jpg
/_*.jpg
# scratch dataURL dumps from headless readback verification
/*.txt
!/prompt.txt
