# RESUME — RCSprint

> Committed handoff so you can pick this project back up on any machine after a `git pull`.
> Regenerated by the **good-night** skill; read by **good-morning**.

**Project · branch:** RCSprint · `main`  ·  **Updated:** 2026-06-16_2330
**Live:** https://aaronjblair.github.io/RCSprint/ (GitHub Pages, deploys on push to `main`) · HEAD `fe215f7`, live 200

## Where we left off
Everything is shipped, built green, and live. The last big effort was the **dirt late-model body**:
- After ~11 hand passes + a **4-agent build-off** + ~18 review agents, we proved **procedural primitives
  can't make a convincing late model**. Shipped the **best procedural** — a smooth **ribbon-skinned wedge**
  (`fe215f7`). It reads as a clean low racing wedge, **not** a true dirt late model.
- **Night is a hard rule** now (the game ships dark-night, game-wide; `?day` is dev-only).

## Next steps (pick up here)
1. **Late model → real 3D model (the real fix).** A genuine late model needs an imported `.glb`, not
   primitives. Drop a low-poly dirt-late-model model at **`public/latemodel.glb`**. The glTF-loader code is
   in git history (commits `064c142`, `13883c0`); re-add it and **fix the blocker**: external GLB bodies
   conflict with the scene's shadow/SSAO/IBL shaders and hang the boot (sampler limit) — re-skin imported
   meshes in the game paint, cap samplers, and/or drop them from shadow casters; load once + instance per
   car; tune `LM_GLB_*` scale/offset/yaw. (No *free* dirt-late-model .glb exists — it's a provided/bought file.)
2. **Car color picker** at the name-entry screen (outstanding backlog).
3. **`docs/POLISH-ROADMAP.md`**: in-race sound + HUD juice, physics load-transfer (make `mass`/suspension
   real), perf instancing/shadow-caster trim.

## What shipped this session (highlights)
- `6d9217f` combine two sessions: #32 opening logo + jogging marshals + booth roof + car classes
- `1ea385b` car-class handling knobs + sprint/late-model skills + `docs/POLISH-ROADMAP.md`
- `14ce451` disable permission prompts + full doc sync + rewritten build prompt
- `ef5e415` Streaker easter-egg updates (code-only, undocumented)
- `4f328d7` / `a891ac3` revert to dark NIGHT + lock it as a hard rule
- `fe215f7` late model = best procedural ribbon-skinned wedge (after the 4-agent build-off)

## Key decisions (+ why)
- **Night is mandatory** — game identity; daylight was a mistake.
- **Late model is a procedural stand-in** until a real `.glb` is integrated (primitives can't get there).
- **Stage specific files, never `git add -A`** — Aaron sometimes runs two Claude sessions in this repo.

## Resume this exact Claude session
`claude --resume d51c30d6-4dbf-45c7-826e-671af8f90a4e`
(Only on the machine holding the local transcript — the session JSONL isn't in the repo. Elsewhere, this
RESUME.md is the handoff.)
