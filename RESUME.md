# RESUME — RCSprint

> Committed handoff so you can pick this project back up on any machine after a `git pull`.
> Regenerated by the **good-night** skill; read by **good-morning**.

**Project · branch:** RCSprint · `main`  ·  **Updated:** 2026-06-16_1923
**Live:** https://aaronjblair.github.io/RCSprint/ (GitHub Pages, deploys on push to `main`)

## Where we left off
Everything is shipped, built green, and live at commit `a891ac3`. Two headline fixes landed last:
- **Night restored + ruled.** Daylight racing (briefly introduced) is gone; the original **dark
  night** look is back and **locked as a hard rule** in `CLAUDE.md` + `prompt.md`/`prompt.txt`/README.
  `?day` is a dev-only preview override.
- **Dirt late model rebuilt from real spec** (DIRTcar/IMCA): body widened to ~0.76× wheelbase so it
  fills out to the tires (integrated fenders), long low nose, chopped roofline, wide 72"-style spoiler.

## Next steps (pick up here)
1. **Late model closer to the reference photo** — real **tumblehome** (curve the body sides inward) +
   **wedge-curved body panels** (extruded/lathed profiles instead of axis-aligned boxes).
2. **Polish roadmap** (`docs/POLISH-ROADMAP.md`): in-race **sound + HUD juice** (collision/lap/finish
   SFX, position toasts, FINAL LAP), physics **load-transfer** (make `mass`/suspension real), perf
   **instancing / mesh-merge** + shadow-caster trim.
3. Outstanding earlier ask: **car color picker** at the name-entry screen.

## What shipped this session (commits)
- `6d9217f` Combine two sessions: #32 opening logo + jogging marshals + booth-roof fix + car classes
- `1ea385b` Car-class handling knobs + sprint/late-model skills + `docs/POLISH-ROADMAP.md`
- `7e87ac0` Perf: shadow-map refreshRate=2
- `14ce451` Disable permission prompts + full doc sync + rewritten build prompt
- `ef5e415` Streaker: down-triangle + infield stop-and-wave (code-only, undocumented) + `?streakcam`
- `c48a926`→`0ba91d4` Late-model bevel/rebuild (6 passes, spec proportions) + `?photofront` dev cam
- `4f328d7`, `a891ac3` Revert to forced dark NIGHT + add it as a hard rule

## Key decisions (+ why)
- **Night is mandatory** — it's the game's identity; daylight was a mistake.
- **Stage specific files, never `git add -A`** — Aaron sometimes runs two Claude sessions in this repo.
- **Streaker stays code-only / undocumented.** Late model is primitives-to-spec, not photoreal.

## Resume this exact Claude session
`claude --resume d51c30d6-4dbf-45c7-826e-671af8f90a4e`
(Only works on the machine holding the local transcript — the session JSONL isn't in the repo. On any
other machine, **this RESUME.md is the handoff**.)
